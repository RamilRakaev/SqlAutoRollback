<Application x:Class="SqlAutoRollback.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:SqlAutoRollback.Helpers"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <helpers:NullToVisibilityConverter x:Key="NullToVisibility"/>
        <helpers:NullToVisibilityConverter x:Key="NullToCollapsed" Invert="False"/>
        <helpers:NullToVisibilityConverter x:Key="NotNullToCollapsed" Invert="True"/>
        <helpers:InverseBooleanConverter x:Key="InverseBool"/>
        <helpers:BoolToBrushConverter x:Key="ErrorBrush">
            <helpers:BoolToBrushConverter.TrueBrush>
                <SolidColorBrush Color="#C42B1C"/>
            </helpers:BoolToBrushConverter.TrueBrush>
            <helpers:BoolToBrushConverter.FalseBrush>
                <SolidColorBrush Color="#1E1E1E"/>
            </helpers:BoolToBrushConverter.FalseBrush>
        </helpers:BoolToBrushConverter>
        <helpers:BooleanToVisibilityConverterEx x:Key="BoolToVis"/>
        <helpers:BooleanToVisibilityConverterEx x:Key="InverseBoolToVis" Invert="True"/>

        <SolidColorBrush x:Key="ToolbarBrush" Color="#F3F3F3"/>
        <SolidColorBrush x:Key="PanelHeaderBrush" Color="#EEEEEE"/>
        <SolidColorBrush x:Key="BorderBrushSubtle" Color="#D0D0D0"/>
        <SolidColorBrush x:Key="ExplorerSelectionBrush" Color="#CAE8FF"/>
        <SolidColorBrush x:Key="ExplorerSelectionBorderBrush" Color="#99CCF3"/>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#F0F0F0"/>

        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="1,2"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Foreground" Value="#1E1E1E"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E5E5E5"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#D0D0D0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#D8D8D8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="88"/>
            <Setter Property="MinHeight" Value="26"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Application.Resources>
</Application>
